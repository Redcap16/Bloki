/*#include <entity/Entity.hpp>

Entity::Entity(BlockManager& blockManager, const AABB& collider) :
	m_Rigidbody(blockManager, collider)
{

}

void Entity::Update(float deltaTime)
{
	m_Rigidbody.Update(deltaTime);
	update(deltaTime);
}*/